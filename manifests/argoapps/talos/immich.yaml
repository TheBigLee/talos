apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: immich
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: apps
  sources:
    - chart: immich
      repoURL: ghcr.io/immich-app/immich-charts
      targetRevision: 0.13.1
      helm:
        values: |
          controllers:
            main:
              strategy: Recreate
              containers:
                main:
                  image:
                    tag: v3.1.0
                  env:
                    DB_PASSWORD:
                      valueFrom:
                        secretKeyRef:
                          name: immich-postgres-user
                          key: password
                    DB_HOSTNAME: psql-pooler.postgresql.svc.cluster.local
                    DB_USERNAME:
                      valueFrom:
                        secretKeyRef:
                          name: immich-postgres-user
                          key: username
                    DB_DATABASE_NAME: immich
          immich:
            persistence:
              library:
                existingClaim: immich-data
          # Requests sized from observed steady-state RSS, limits from
          # observed peaks plus headroom. Without these all three ran as
          # BestEffort and were the first things Talos' OOM killer took.
          valkey:
            enabled: true
            controllers:
              main:
                containers:
                  main:
                    resources:
                      requests:
                        cpu: 25m
                        memory: 32Mi
                      limits:
                        memory: 128Mi
          server:
            enabled: true
            controllers:
              main:
                containers:
                  main:
                    resources:
                      requests:
                        cpu: 200m
                        memory: 1Gi
                      limits:
                        memory: 2560Mi
          machine-learning:
            persistence:
              cache:
                storageClass: hcloud-volumes
            controllers:
              main:
                containers:
                  main:
                    resources:
                      requests:
                        cpu: 100m
                        memory: 512Mi
                      limits:
                        memory: 2Gi
    - path: manifests/apps/talos/immich
      repoURL: https://github.com/TheBigLee/talos.git
      targetRevision: HEAD
  destination:
    namespace: immich
    server: https://kubernetes.default.svc
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
