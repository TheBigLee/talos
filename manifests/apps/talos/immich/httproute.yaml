---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich-route
spec:
  parentRefs:
  - name: traefik-gateway
    namespace: traefik-system
  hostnames:
  - "immich.bigli.io"
  rules:
  - backendRefs:
    - name: immich-server
      port: 2283
